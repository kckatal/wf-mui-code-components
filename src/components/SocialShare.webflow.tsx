import { declareComponent } from "@webflow/react";
//import { props } from "@webflow/data-types";
import { DropdownShareButton } from "./SocialShare";

export default declareComponent(DropdownShareButton, {
  name: "Social Share",
  description: "A social share button",
});